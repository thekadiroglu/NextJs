'use client';
import React from 'react'

const AddToCard = () => {
  return (
    <div>
      <button onClick={() => console.log('click')}>Ad to Cart</button>
    </div>
  )
}

export default AddToCard
